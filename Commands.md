Peruskomentoja:

$ docker help
$ docker COMMAND --help
$ docker version
$ docker info
$ docker search (image)
$ docker run -t -i (image)
$ docker ps
$ docker images
$docker run -t -i (image):latest
$ docker attach (ID)
$stop <ContainerId>
 
